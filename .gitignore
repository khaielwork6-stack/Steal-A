# Project place file
/Steal A.rbxlx

# Roblox Studio lock files
/*.rbxlx.lock
/*.rbxl.lock

sourcemap.json

# Video reference frames for the map rebuild (local only, not committed)
/reference/

# Static analysis cache (tools/check.sh downloads it)
/.luau-types/
