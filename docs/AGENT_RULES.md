You are building one feature area for the Roblox (Luau) game "Steal & Run!", a Steal-a-Brainrot-style museum heist game (players steal sealed containers from 12 museum zones past lasers and guards, carry them home, the containers incubate and are revealed in a casino-style roulette, displayed items earn cash per second; Speed is trained on treadmills; 7 players per server, each with a plot/base). You work in an isolated git worktree (Rojo project: src/server, src/client, src/shared). Several other agents build other features in parallel; the lead merges everything and tests in Roblox Studio.

READ FIRST: docs/START_HERE.md (architecture), README.md, and the docs/handoff-parts/*.md notes from the fix pass that just landed (data.md, security-gameplay.md, client.md, world-health.md, monetization.md). HANDOFF.md is the long historical design log (grep it, don't edit it).

RULES
1. Do NOT use any Roblox Studio MCP tools, and never launch Roblox Studio. You cannot run the game: be careful, re-read your code, follow existing patterns.
2. Static check: run `bash tools/check.sh` before (baseline) and after. The "hard errors" section must be EMPTY (it is empty now); do not add type errors where avoidable (`--!strict`, typed locals, `:: Type` casts, IsA checks). `selene src` must report 0 errors.
3. Line endings: preserve each existing file's endings; new files may be LF. Prefer Edit/Write tools; NEVER `sed -i`. No reformatting / StyLua runs.
4. Style: tabs; the codebase's "why"-comment style; `--!strict` on new modules. Services expose `X.start()` and are wired in src/server/init.server.luau; client controllers expose `X.start()` and are started in src/client/init.client.luau with the existing `startLater("Name", fn)` pattern. Config lives in src/shared/Config (add validate.luau checks for new config in ONE small block). New remotes: ONE contiguous block appended at the END of Remotes.NAMES (src/shared/Remotes.luau) with a comment header naming your area; validate every remote argument server-side (see src/shared/Util/Validate.luau and RateLimiter.luau) and rate-limit.
5. Shared files that every agent touches (init.server.luau, init.client.luau, Remotes.luau, DataService.luau, GameConfig.luau, validate.luau, StateService.luau, HUDLayoutController.luau): keep edits small, self-contained, commented, and appended where possible. New profile fields: add them to the Profile type, defaultProfile and reconcile in DataService.luau in one clearly marked block per area; do NOT bump the schema version.
6. Existing infrastructure you must use (don't reinvent):
   - Client state: `src/client/StateStore.luau` (subscribe / onKeyChanged / get). Never connect to the StatePush remote directly. Server state fields: StateService has a provider/registration mechanism for extra fields - read it and add your field through it (keep payloads small; send large or rarely-changing data on your own remote only when it changes, like InventoryPush).
   - Durable delivery to offline/other-server players: `MailboxService.send(userId, entry)` (see its header for entry kinds: item, cash, speed).
   - Purchases: MonetizationService / MonetizationConfig. New Robux products or gamepasses: add config rows with id 0 (placeholder) and make the feature degrade gracefully (hidden/disabled) while the id is 0; list every id the owner must create in your notes. Products are granted ONLY in ProcessReceipt (follow the existing receipt pattern and its idempotency/save rules). Respect `MonetizationService.policy(player)` (PolicyService): no paid random items where restricted; no paid trading where `IsPaidItemTradingAllowed` is false.
   - Cash: EconomyService.award / spend with a source tag. Speed grants: follow how GroupGiftService/SpinWheelService grant Speed.
   - Anti-cheat: any server teleport of a character must call `MovementService.allowTeleport(player)` first.
   - Effects/Reduced Effects & Low Graphics: `src/shared/Util/Effects.luau` - honour it in any new VFX/animation.
   - UI: the purchased UI pack. HUD nav buttons live in PlayerGui.MainUI.Buttons.Left (TextButtons Shop/Index/Settings in a UIGridLayout); panels live in MainUI.Frames (Shop, Upgrades, Settings, Index, Sell). A place-owned script (mirrored in place/StarterGui/MainUI/LocalScript.client.luau - read it) wires any MainUI button to the Frame with the same name (slide-up, blur, one panel at a time, close via Frame.Top.Frame.Close). HUDLayoutController already clones nav buttons (Upgrades/Storage openers) - follow that pattern for a new HUD button. src/shared/Util/ShopKit.luau clones pack components (product card, section header, top bar) - use it for panels/cards so new UI matches. Build new panels by cloning an existing Frame skeleton at runtime (e.g. Frames.Index) and replacing its content; never mutate the templates. Obey the pack rules documented in ShopKit (UIAnim.claimButton before binding). Mobile: touch targets >= 44px effective, check small screens (MainUI has a UIScale 0.72-0.85 on phones), use Device.isTouch().
   - Debug commands: new Studio test commands go in `src/server/Services/DebugCommands/<Area>.luau` returning `{ [name] = function(player, ...) end }`. Any test that changes the player's profile must restore it even on error (Studio writes to the owner's REAL DataStore).
7. ART/ASSETS: you cannot create images, meshes, sounds or badge/gamepass/product entries. Use clean placeholders (text-only or existing pack icons, simple parts) behind config keys (e.g. an `Icon = "rbxassetid://0"` field that the UI skips when 0), and write a precise, self-contained prompt for Codex (another AI that can generate images/meshes into Roblox Studio and upload them) to `docs/codex-prompts/<area>.md`: for each asset: name, where it's used, exact size/aspect, style (match the existing UI pack: glossy cartoon, thick outline, bright gradients; 3D: stylised realism like the museum loot), and exactly where Codex must put the resulting asset id (config file + key). Keep that file focused and actionable.
8. Notes for the lead: `docs/handoff-parts/<area>.md` - what you built, design decisions and values, every owner action (Creator Dashboard: badges, gamepasses, products, notification templates, API keys; Studio: anything to place), and a precise Studio test plan using your debug commands.
9. Finish: `git add -A && git commit -m "<clear message>"` (do not push). Final message under ~600 words: branch name, files changed, what's placeholder, what you couldn't do, what the lead must verify.
10. OUT OF SCOPE (owner said no): rebirth/prestige, base shield/lock, server luck boost, any change to the Zone 8+ progression wall (treadmill output, zone Speed gates, zone income curves, loot weights). Don't touch other agents' features.
